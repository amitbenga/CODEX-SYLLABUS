[project]
name = "notebooklm-assistant"
version = "0.1.0"
description = "MCP server that brings Google NotebookLM into Claude Desktop and Claude Code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.4",
    "notebooklm-py[browser]>=0.3.1",
]
